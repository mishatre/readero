
import styles from './styles.module.scss';

interface IReaderControlsProps {

}

const ReaderControls = ({ }: IReaderControlsProps) => {
    return (
        <div className={styles.container}>

        </div>
    );
};

export default ReaderControls;